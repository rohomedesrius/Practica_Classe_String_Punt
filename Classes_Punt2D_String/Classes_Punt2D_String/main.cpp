#include <iostream>
#include "point2D.h"
#include "projString.h"

using namespace std;

int main(int argc, char** argv){

	//Prova Distance To
	//----------------------------------------------------------------
	/*
	point2D point3;
	point2D point4;
	float a = 0, b = 0, d = 0;

	point3.SetZero();
	point4.SetZero();

	point3.x = 3;
	point3.y = 5;

	point4.x = 2;
	point4.y = -1;

	a = (point3.x + point4.x) * (point3.x + point4.x);
	b = (point3.y + point4.y) * (point3.y + point4.y);

	d = sqrt(a + b);

	printf("%f", d);
	//----------------------------------------------------------------
	*/

	//printf("%s", str_2);

	//getchar();
}